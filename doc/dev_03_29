仔细想想看，几乎该写的都写完了，也要给老师申请软件著作权了，版本也是强行升级到了1.0

剩下的都是一些我不怎么想写或者当前环境下没有必要写的东西：
TODO:
1、关于一些类似于用户个人信息维护、文件信息维护、关系信息维护等增删改查的我就懒写了，太墨迹。
2、关于锁屏和登出操作
3、左侧菜单栏分类查看，以及类别的维护
    ->类别应该是自动识别的，例如某个文件的拓展名符合需求即可
    ->类别不应该手动维护
4、根据不同类别预览文件的功能
5、还有搜索功能，建立索引用AOP切入。
6、复制功能，这个需要复制SQL语句……实在是太冗余了。
7、还有表格选择的时候，至今都还有一些Bug：
    ->右键逻辑
        ->如果当前没有被选择的，那么右键所对应的行，应该被选择。
        ->如果当前有已经被选择的，那么右键仅仅弹出菜单。
    ->同时关于左键逻辑
        ->放弃点击表格项就选中的效果，还是让用户一个一个勾选比较好。
   不过这个实在是不怎么太重要，先这样吧。
8、还有现在删除就是直接删，这个有点太暴力了。应当调用disable方法。
    ->parentId = '-2:temp#{原parentId}';
    ->status = disable;
    同时如果是文件夹，那么所有的child的状态都应该改为disable;
    update hander set status = disable where parentId = #{上面的handerId}
9、回收站的逻辑应该是:
    ->显示所有的parentId为{'%-2'}的项，当做一个根目录
    ->右键菜单仅出现彻底删除和恢复。
    ->上方菜单仅出现清空回收站
10、右侧的通知栏要做什么我还没想好，先放着吧。
11、个人资料和会员中心还需要另外做个页面，拉鸡八倒吧。
12、爱我草还有个Logo没做……